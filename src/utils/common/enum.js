
const systemRole={
    ADMIN: 'admin',
    USER: 'user'
}
Object.freeze(systemRole)

const status={
    COMPELTED:"completed",
    INCOMPELTED:"incompleted",
} 
Object.freeze(status)
export{
    status,
    systemRole
}